using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NoClumping : MonoBehaviour
{
    // public List<Particle> particles;
    // public float repulsionDistance = 0.2f;
    // public float repulsionStrength = 0.005f;
    //
    // private void Update()
    // {
    //     if (particles != null && particles.Count > 0)
    //     {
    //         ApplyNoClumping();
    //     }
    // }
    // private void ApplyNoClumping()
    // {
    //     for (int i = 0; i < particles.Count; i++)
    //     {
    //         Particle p1 = particles[i];
    //         Vector2 pos1 = p1.GetPosition();
    //
    //         for (int j = i + 1; j < particles.Count; j++)
    //         {
    //             Particle p2 = particles[j];
    //             Vector2 pos2 = p2.GetPosition();
    //
    //             float distance = Vector2.Distance(pos1, pos2);
    //             if (distance < repulsionDistance && distance > 0.0001f)
    //             {
    //                 Vector2 repulsion = (pos1 - pos2).normalized * repulsionStrength;
    //                 p1.ApplyForce(repulsion);
    //                 p2.ApplyForce(-repulsion);
    //             }
    //         }
    //     }
    // }
    //
    
    
    
    
    
}